.. jinja:: project_info

   {{ project }}
   =============

   {{ project }} is a CLI tool (``grovems run --config ...``), not primarily an
   importable library -- see :doc:`notes/quickstart` for usage. The package reference
   below covers only the top-level package metadata; ``runner.py``/``rescoring/``/
   ``psa/``/``iforest/`` are internal orchestration, not a public API.

   .. automodule:: {{ project }}
      :members:
